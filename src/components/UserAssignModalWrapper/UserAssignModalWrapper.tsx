import Modal from "../Modal/Modal"

const UserAssignModalWrapper = ({setUserAssign}
: {setUserAssign: (value: boolean) => void }) => {
    return(
        <Modal setUserAssign={setUserAssign}>
            kjcndjn
        </Modal>
    )
}

export default UserAssignModalWrapper